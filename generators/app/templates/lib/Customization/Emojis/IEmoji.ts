import IEmojiOptions from "./IEmojiOptions";

/**
 * Represents an emoji.
 *
 * Please keep in mind to provide the files using a FilesInstruction.
 */
export default interface IEmoji extends Required<IEmojiOptions>
{
}