<?xml version="1.0" encoding="UTF-8" ?>
<package name="<%= Identifier %>" xmlns="http://www.woltlab.com" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.woltlab.com http://www.woltlab.com/XSD/vortex/package.xsd">
    <packageinformation><% for (const locale in DisplayName) { %>
        <packagename<% if (locale !== 'en') { %> language="<%= locale %>"<% } %>>
            <![CDATA[<%- DisplayName[locale] %>]]>
        </packagename><% } %><% for (const locale in Description) { %>
        <packagedescription<% if (locale !== 'en') { %> language="<%= locale %>"<% } %>>
            <![CDATA[<%- Description[locale] %>]]>
        </packagedescription><%}%>
        <version><%= Version %></version>
        <date><%= (Date.getFullYear() + '-' + (Date.getMonth() + 1).toString().padStart(2, '0') + '-' + Date.getDate().toString().padStart(2, '0')) %></date>
    </packageinformation>
    <authorinformtion>
        <author><%= Author.Name %></author>
        <authorurl><%= Author.URI %></authorurl>
    </authorinformtion>
    <requiredpackages>
        <requiredpackage minversion="3.0.0">com.woltlab.wcf</requiredpackage>
    </requiredpackages><%
    %><% if (InstallInstruction) { %>
        <instructions type="install"><%
            %><%- include("instructions.xml", { InstructionCollection: InstallInstruction }); %>
        </instructions><% } %><%
    %><% for (let instructionCollection in UpdateInstructions) { %>
        <instructions type="update" fromversion="<%= instructionCollection.FromVersion %>"><%
            %><%- include("instructions.xml", { InstructionCollection: instructionCollection }); %>
        </instructions><% } %>
</package>