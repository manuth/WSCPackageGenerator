{
  "name": <%- JSON.stringify(Name.toLowerCase()) %>,
  "version": "0.0.0",
  "description": <%- JSON.stringify(Description) %>,
  "author": {
    "name": <%- JSON.stringify(Author) %><% if (HomePage.length > 0) { %>,
    "url": <%- JSON.stringify(HomePage) %><% } %>
  },
  "devDependencies": {
    "woltlab-compiler": "^1.0.0"
  },
  "engines": {
    "npm": ">= 4.0.0"
  },
  "dependencies": {},
  "scripts": {
    "watch": "tsc -watch -p ./",
    "compile": "tsc -p ./",
    "build": "npm run compile && node lib/Program.js"
  },
  "license": "Apache-2.0"
}