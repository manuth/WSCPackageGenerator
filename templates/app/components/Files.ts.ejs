import Path = require("path");
import { ApplicationFileSystemInstruction } from "woltlab-compiler";

export = new ApplicationFileSystemInstruction(
    {
        Application: <%- JSON.stringify(Application) %>,
        Source: <%- MakePackagePath(FilesDirectory) %>
    });