import Path = require("path");
import { TemplateInstruction } from "woltlab-compiler";

export = new TemplateInstruction(
    {
        Application: <%- JSON.stringify(Application) %>,
        Source: <%- MakePackagePath(TemplateRoot) %>
    });