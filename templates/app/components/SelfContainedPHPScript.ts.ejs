import { join } from "path";
import { SelfContainedPHPInstruction } from "@manuth/woltlab-compiler";

export = new SelfContainedPHPInstruction(
    {
        Application: <%- JSON.stringify(Application) %>,
        Source: <%- MakePackagePath(PHPSource) %>,
        Destination: <%- JSON.stringify(PHPFile) %>
    });