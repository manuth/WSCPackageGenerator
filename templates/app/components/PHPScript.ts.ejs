import { PHPInstruction } from "@manuth/woltlab-compiler";

export = new PHPInstruction(
    {
        Application: <%- JSON.stringify(Application) %>,
        FileName: <%- JSON.stringify(PHPFile) %>
    });