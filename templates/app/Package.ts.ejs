import Path = require("path");
import { IPackageOptions } from "<%- RelativeSourceRoot %>System/PackageSystem/IPackageOptions";
import { ThemeInstructionCollection } from "<%- RelativeSourceRoot %>System/Customization/Presentation/Themes/ThemeInstructionCollection";
import { RequiredPackageDescriptor } from "<%- RelativeSourceRoot %>System/PackageSystem/RequiredPackageDescriptor";

let pkg: IPackageOptions = {
    Identifier: <%- JSON.stringify(Identifier) %>,
    Name: <%- JSON.stringify(Name) %>,
    DisplayName: {
        inv: <%- JSON.stringify(DisplayName) %>
    },
    Version: "0.0.0",
    Author: {
        Name: <%- JSON.stringify(Author) %>,
        URL: <%- JSON.stringify(HomePage) %>
    },
    License: null,
    CreationDate: new Date(<%- JSON.stringify(CreationDate) %>),
    Description: {
        inv: <%- JSON.stringify(Description) %>
    },
    InstallSet: {
        Instructions: [
            <%- Instructions.join(`,
            `) %>
        ]
    },
    RequiredPackages: [
        new RequiredPackageDescriptor({
            Identifier: "com.woltlab.wcf",
            MinVersion: "3.0.0"
        })
    ]
};

export = pkg;