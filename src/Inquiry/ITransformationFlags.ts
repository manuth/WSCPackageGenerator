/**
 * Provides flags for performing transformations.
 */
export interface ITransformationFlags
{
    /**
     * A value indicating whether the final transformation is being performed.
     */
    isFinal?: boolean;
}
